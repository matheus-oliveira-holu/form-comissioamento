 rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true;
    }
  }
  
  // Permite gravação apenas para usuários autenticados
  match /{document=**} {
      allow write: if request.auth !=null;
    }
}